 don't forget ANT_HOME and path for ant needs to be set for builsing deploy-local-install
 in eclispe


 
 
 
 cd /bioimaging/home/gvf/dev/apps/caarray/jboss-4.0.5.GA/bin
 
 edit run.sh ( replace  ${grid.static.hostname} in section:if [ "x$LAUNCH_JBOSS_IN_BACKGROUND" = "x" ]; then
 with cox.lbl.gov 
 
 
 -----
 
 currently running CARPLA.xml to restore/write
 --set launch to refresh target ( caRPLA project)
 then run deploy-local-instlall or copy-caarray2-application
 in top level build.xml in caRPLA
 
 
 38080
 
 stil have problems also building grid...don;t need it, so skipped it.
 
 edits tagged with carpla