<!-- *********************************************************************** -->
<!-- ** PROJECT:   caArray Java API Client Examples                       ** -->
<!-- *********************************************************************** -->

<project name="caarrayJavaClientExamples_v1_0" default="build" basedir=".">
    <!-- ******************************************************************* -->
    <!-- ** SERVER CONNECTION PROPERTIES                                  ** -->
    <!-- ******************************************************************* -->
    <property name="server.hostname" value="array-dev.nci.nih.gov" />
    <property name="server.jndi.port" value="8080" />

    <!-- ******************************************************************* -->
    <!-- ** PROPERTIES NEEDED FOR BUILD                                   ** -->
    <!-- ******************************************************************* -->
    <property name="src.dir" value="${basedir}/src" />
    <property name="lib.dir" value="${basedir}/lib" />
    <property name="classes.dir" value="${basedir}/classes" />

    <!-- ******************************************************************* -->
    <!-- ** BUILD TARGETS                                                 ** -->
    <!-- ******************************************************************* -->
    <target name="clean">
        <delete dir="${classes.dir}" />
    </target>

    <target name="build">
        <mkdir dir="${classes.dir}" />
        <javac srcdir="${src.dir}" destdir="${classes.dir}" debug="true">
            <classpath>
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
        </javac>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** SEARCH FOR EXPERIMENTS                                        ** -->
    <!-- ******************************************************************* -->
    <target name="search_experiments_by_criteria" depends="build">
        <java classname="caarray.client.examples.java.SearchExperimentsByCriteria">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="search_experiments_by_keyword" depends="build">
        <java classname="caarray.client.examples.java.SearchExperimentsByKeyword">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** SEARCH FOR BIOMATERIALS                                       ** -->
    <!-- ******************************************************************* -->
    <target name="search_biomaterials_by_criteria" depends="build">
        <java classname="caarray.client.examples.java.SearchBiomaterialsByCriteria">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="search_biomaterials_by_keyword" depends="build">
        <java classname="caarray.client.examples.java.SearchBiomaterialsByKeyword">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** SELECT FILES                                                  ** -->
    <!-- ******************************************************************* -->
    <target name="select_files" depends="build">
        <java classname="caarray.client.examples.java.SelectFiles">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** DOWNLOAD FILE CONTENTS                                        ** -->
    <!-- ******************************************************************* -->
    <target name="download_file" depends="build">
        <java classname="caarray.client.examples.java.DownloadFile">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="download_file_zip" depends="build">
        <java classname="caarray.client.examples.java.DownloadFileZipFromExperiment">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="download_multiple_files" depends="build">
        <java classname="caarray.client.examples.java.DownloadMultipleFiles">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** DOWNLOAD DATA COLUMNS                                         ** -->
    <!-- ******************************************************************* -->
    <target name="download_data_columns_from_file" depends="build">
        <java classname="caarray.client.examples.java.DownloadDataColumnsFromFile">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="download_data_columns_from_hyb" depends="build">
        <java classname="caarray.client.examples.java.DownloadDataColumnsFromHybridizations">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** DOWNLOAD SAMPLE ANNOTATIONS                                   ** -->
    <!-- ******************************************************************* -->
    <target name="download_sample_annotations" depends="build">
        <java classname="caarray.client.examples.java.DownloadSampleAnnotationsForHybridizations">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** LOOKUP ENTITIES IN PREPARATION FOR MAKING OTHER API CALLS     ** -->
    <!-- ******************************************************************* -->
    <target name="lookup_entities" depends="build">
        <java classname="caarray.client.examples.java.LookUpEntities">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** DOWNLOAD A MAGE-TAB EXPORT OF AN EXPERIMENT                   ** -->
    <!-- ******************************************************************* -->
    <target name="download_magetab_export" depends="build">
        <java classname="caarray.client.examples.java.DownloadMageTabExport">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <target name="download_magetab_export_with_data" depends="build">
        <java classname="caarray.client.examples.java.DownloadMageTabExportWithDataFiles">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

    <!-- ******************************************************************* -->
    <!-- ** DOWNLOAD ARRAY DESIGN FILES                                   ** -->
    <!-- ******************************************************************* -->
    <target name="download_array_design" depends="build">
        <java classname="caarray.client.examples.java.DownloadArrayDesignForHybridization">
            <classpath>
                <pathelement path="${classes.dir}" />
                <fileset dir="${lib.dir}">
                    <include name="**/*.jar" />
                </fileset>
            </classpath>
            <sysproperty key="server.hostname" value="${server.hostname}" />
            <sysproperty key="server.jndi.port" value="${server.jndi.port}" />
        </java>
    </target>

</project>
