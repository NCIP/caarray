-Installer has only been tested on Windows XP
-Close any open applications, including web browsers
-Disable any Antivirus scanners
-Shutdown MySQL and JBoss (java.exe) processes (Task Manager)
-Enable firewall ports to run application (e.g. Windows Firewall).

In case application doesn't automatically open in web browser, go to http://localhost:8080/caarray/


