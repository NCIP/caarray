package gov.nih.nci.caarray.services.external.v1_0.grid.service.globus;


import java.rmi.RemoteException;
import java.util.HashMap;
import java.util.Map;
import java.io.File;
import javax.security.auth.Subject;
import javax.xml.namespace.QName;
import javax.xml.rpc.handler.MessageContext;

import gov.nih.nci.cagrid.introduce.servicetools.security.AuthorizationExtension;
import org.globus.wsrf.impl.security.authorization.exceptions.AuthorizationException;
import org.globus.wsrf.impl.security.authorization.exceptions.CloseException;
import org.globus.wsrf.impl.security.authorization.exceptions.InitializeException;
import org.globus.wsrf.impl.security.authorization.exceptions.InvalidPolicyException;
import org.globus.wsrf.security.authorization.PDP;
import org.globus.wsrf.security.authorization.PDPConfig;
import org.globus.wsrf.config.ContainerConfig;
import org.w3c.dom.Node;


/** 
 * DO NOT EDIT:  This class is autogenerated!
 *
 * This is a PDP for use with the globus authorization callout.
 * This class will have a authorize method for each method on this grid service.
 * The method is responsible for making any authorization callouts required to satisfy the 
 * authorization requirements placed on each method call.  Each method will either return
 * upon a successful authorization or will throw an exception upon a failed authorization.
 * 
 * @created by Introduce Toolkit version 1.5
 * 
 */
public class CaArraySvc_v1_0Authorization implements PDP {

	public static final String SERVICE_NAMESPACE = "http://grid.v1_0.external.services.caarray.nci.nih.gov/CaArraySvc_v1_0";
	
	Map authorizationClassMap = new HashMap();
	
	
	public CaArraySvc_v1_0Authorization() {
	}
	
	protected String getServiceNamespace(){
		return SERVICE_NAMESPACE;
	}
	
	public static String getCallerIdentity() {
		String caller = org.globus.wsrf.security.SecurityManager.getManager().getCaller();
		if ((caller == null) || (caller.equals("<anonymous>"))) {
			return null;
		} else {
			return caller;
		}
	}
					
	public void authorizeGetServiceSecurityMetadata(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForExperiments(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetAllPrincipalInvestigators(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateExperiments(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetFileContentsTransfer(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetDataSet(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForBiomaterials(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForHybridizations(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForExperimentsByKeyword(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForFiles(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForBiomaterialsByKeyword(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetMageTabExport(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchForQuantitationTypes(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeSearchByExample(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetAnnotationSet(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetTermsForCategory(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetAllCharacteristicCategories(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateExperimentsByKeyword(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateBiomaterials(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateBiomaterialsByKeyword(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateHybridizations(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateFiles(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeEnumerateByExample(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetMultipleResourceProperties(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeGetResourceProperty(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   				
	public void authorizeQueryResourceProperties(Subject peerSubject, MessageContext context, QName operation) throws AuthorizationException {
		
	}
	   
	
	public boolean isPermitted(Subject peerSubject, MessageContext context, QName operation)
		throws AuthorizationException {
		
		if(!operation.getNamespaceURI().equals(getServiceNamespace())){
		  return false;
		}
		if(operation.getLocalPart().equals("getServiceSecurityMetadata")){
			authorizeGetServiceSecurityMetadata(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForExperiments")){
			authorizeSearchForExperiments(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getAllPrincipalInvestigators")){
			authorizeGetAllPrincipalInvestigators(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateExperiments")){
			authorizeEnumerateExperiments(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getFileContentsTransfer")){
			authorizeGetFileContentsTransfer(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getDataSet")){
			authorizeGetDataSet(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForBiomaterials")){
			authorizeSearchForBiomaterials(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForHybridizations")){
			authorizeSearchForHybridizations(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForExperimentsByKeyword")){
			authorizeSearchForExperimentsByKeyword(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForFiles")){
			authorizeSearchForFiles(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForBiomaterialsByKeyword")){
			authorizeSearchForBiomaterialsByKeyword(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getMageTabExport")){
			authorizeGetMageTabExport(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchForQuantitationTypes")){
			authorizeSearchForQuantitationTypes(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("searchByExample")){
			authorizeSearchByExample(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getAnnotationSet")){
			authorizeGetAnnotationSet(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getTermsForCategory")){
			authorizeGetTermsForCategory(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getAllCharacteristicCategories")){
			authorizeGetAllCharacteristicCategories(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateExperimentsByKeyword")){
			authorizeEnumerateExperimentsByKeyword(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateBiomaterials")){
			authorizeEnumerateBiomaterials(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateBiomaterialsByKeyword")){
			authorizeEnumerateBiomaterialsByKeyword(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateHybridizations")){
			authorizeEnumerateHybridizations(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateFiles")){
			authorizeEnumerateFiles(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("enumerateByExample")){
			authorizeEnumerateByExample(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getMultipleResourceProperties")){
			authorizeGetMultipleResourceProperties(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("getResourceProperty")){
			authorizeGetResourceProperty(peerSubject, context, operation);
			return true;
		} else if(operation.getLocalPart().equals("queryResourceProperties")){
			authorizeQueryResourceProperties(peerSubject, context, operation);
			return true;
		} 		
		return false;
	}
	

	public Node getPolicy(Node query) throws InvalidPolicyException {
		return null;
	}


	public String[] getPolicyNames() {
		return null;
	}


	public Node setPolicy(Node policy) throws InvalidPolicyException {
		return null;
	}


	public void close() throws CloseException {


	}


	public void initialize(PDPConfig config, String name, String id) throws InitializeException {
    	try{
    		String serviceName = (String)config.getProperty(name, "serviceName");
    	    String etcPath = ContainerConfig.getBaseDirectory() + File.separator + (String)config.getProperty(name, "etcDirectoryPath");

    	
    	} catch (Exception e){
        	throw new InitializeException(e.getMessage(),e);
		}
	}
	
	
}
