validator.assertFalse=assertion failed
validator.assertTrue=assertion failed
validator.future=(fieldName) must be a future date
validator.length=(fieldName) length must be between {min} and {max}
validator.max=(fieldName) must be less than or equal to {value}
validator.min=(fieldName) must be greater than or equal to {value}
validator.notNull=(fieldName) must be set
validator.past=(fieldName) must be a past date
validator.pattern=(fieldName) is in an invalid pattern.
validator.range=(fieldName) must be between {min} and {max}
validator.size=(fieldName) size must be between {min} and {max}
validator.email=(fieldName) is not a well-formed email address
validator.digits=Numeric value out of bounds (<{integerDigits} digits>.<{fractionalDigits} digits> expected)
validator.creditCard=(fieldName) is an invalid credit card number
validator.ean=(fieldName) is an invalid EAN
validator.invalid=fieldName) is invalid
validator.authenticationFailed=Authentication failed. Please try again
validator.unique=(fieldName) is already in use
validator.minLength=(fieldName) must be at least ${minLength} characters long

validator.uniqueConstraint=The specified field(s) value is not unique.

biomaterial.externalId.uniqueConstraint=A biomaterial with the same external id value already exists
term.uniqueConstraint=A term with the same value and source or accession value and source already exists
designElementList.uniqueConstraint=A design element with the same LSID value already exists
category.uniqueConstraint=A category with the same name and term source already exists
termSource.uniqueConstraint=A term source with the same name and version or url and version already exists
organism.uniqueConstraint=A n organism with the same name and source already exists
term.source.notNull=You must select a term source or create a new source.
caarrayfile.uniqueConstraint=A caarray file with the same name and project already exists